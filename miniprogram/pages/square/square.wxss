/* pages/square/square.wxss */
page {
  background-color: #F6F6F6;
  height: 100%;
}

.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  box-sizing: border-box;
}

/* Header */
.header-section {
  width: 100%; /* Ensure full width */
  background-color: #9C1F22; /* Slightly darker/richer red */
  padding: 20rpx 0 80rpx 0; /* Extra bottom padding for search bar overlap */
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
}

.header-slogan {
  color: #fff;
  font-size: 28rpx;
  margin-bottom: 20rpx;
  opacity: 0.9;
  letter-spacing: 2rpx;
  font-style: italic;
}

.toggle-container {
  margin-bottom: 10rpx;
}

.toggle-pill {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50rpx;
  padding: 6rpx;
  display: flex;
  width: 320rpx; /* Fixed width */
  backdrop-filter: blur(5px);
}

.toggle-btn {
  flex: 1;
  text-align: center;
  padding: 12rpx 0;
  border-radius: 40rpx;
  font-size: 30rpx;
  color: rgba(255,255,255,0.8);
  font-weight: 500;
  transition: all 0.3s;
}

.toggle-btn.active {
  background-color: #fff;
  color: #9C1F22;
  font-weight: bold;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.2);
}

/* Search Bar */
.search-container {
  padding: 0 40rpx;
  margin-top: -50rpx; /* Negative margin to pull up */
  position: relative;
  z-index: 10;
  margin-bottom: 30rpx;
}

.search-bar {
  background-color: #fff;
  border-radius: 50rpx; /* Fully rounded */
  padding: 16rpx 20rpx 16rpx 30rpx; /* Right padding smaller for button */
  display: flex;
  align-items: center;
  box-shadow: 0 10rpx 30rpx rgba(156, 31, 34, 0.15); /* Reddish soft shadow */
}

.search-icon {
  margin-right: 20rpx;
  opacity: 0.8;
}

.search-bar input {
  flex: 1;
  font-size: 28rpx;
  color: #333;
  height: 60rpx;
  line-height: 60rpx;
}

.search-placeholder {
  color: #bbb;
  font-size: 28rpx;
}

.search-btn {
  background: linear-gradient(135deg, #9C1F22, #D32F2F);
  color: #fff;
  font-size: 26rpx;
  padding: 12rpx 32rpx;
  border-radius: 30rpx;
  font-weight: bold;
  margin-left: 20rpx;
  box-shadow: 0 4rpx 10rpx rgba(156, 31, 34, 0.3);
  transition: all 0.2s;
}

.search-btn:active {
  transform: scale(0.95);
  opacity: 0.9;
}

/* List */
.content-list {
  flex: 1;
  padding: 0 30rpx;
  box-sizing: border-box;
  overflow: hidden; /* Fix for flex scroll-view */
  height: 0; /* Critical for scroll-view in flex column */
  width: 100%;
}

.empty-tip {
  text-align: center;
  color: #999;
  margin-top: 100rpx;
}

.list-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.03);
  position: relative;
  transition: all 0.2s;
}

.list-card:active {
  transform: scale(0.98);
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 16rpx;
}

.card-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  flex: 1;
  margin-right: 20rpx;
  line-height: 1.4;
}

.card-tag {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: #f0f0f0;
  color: #666;
  white-space: nowrap;
}

/* Tag colors - using English keys */
.tag-life { background: #E8F5E9; color: #2E7D32; }
.tag-study { background: #E3F2FD; color: #1565C0; }
.tag-emotion { background: #FCE4EC; color: #C2185B; }
.tag-campus { background: #FFF3E0; color: #E65100; }
.tag-default { background: #f5f5f5; color: #999; }

.card-content {
  font-size: 28rpx;
  color: #666;
  line-height: 1.5;
  margin-bottom: 24rpx;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2; /* Limit to 2 lines */
  overflow: hidden;
}

.card-footer {
  display: flex;
  justify-content: space-between;
  font-size: 24rpx;
  color: #999;
}

/* Bottom Footer Area */
.footer-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #fff;
  border-radius: 40rpx 40rpx 0 0;
  padding: 40rpx 40rpx 30rpx; /* 增加顶部内边距 */
  box-shadow: 0 -5rpx 20rpx rgba(0,0,0,0.08); /* Stronger shadow for floating effect */
  padding-bottom: calc(env(safe-area-inset-bottom) + 180rpx); /* Leave space for TabBar */
  z-index: 99;
}

.category-scroll {
  display: flex;
  white-space: nowrap;
  margin-bottom: 30rpx;
  width: 100%;
  height: 85rpx; /* 增加高度 */
  align-items: center; /* 滚动容器内项目垂直居中 */
}

.category-item {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 54rpx; /* 固定高度 */
  line-height: normal; /* 重置行高 */
  padding: 0 30rpx;
  margin-right: 20rpx;
  font-size: 26rpx;
  background: #f5f5f5;
  color: #666;
  border-radius: 30rpx;
  transition: all 0.3s;
  box-sizing: border-box; 
  vertical-align: middle;
}

.category-item.active {
  background: #9C1F22;
  color: #fff;
  font-weight: bold;
}

.action-btn {
  background: linear-gradient(135deg, #9C1F22, #D32F2F); /* 更加美观的渐变红 */
  color: #fff;
  border-radius: 50rpx;
  font-size: 34rpx;
  font-weight: 600;
  height: 90rpx;
  width: 100% !important;
  box-shadow: 0 10rpx 25rpx rgba(156, 31, 34, 0.3); /* 柔和的彩色阴影 */
  border: none;
  
  /* Flex 布局确保文字绝对居中 */
  display: flex !important;
  align-items: center;
  justify-content: center;
  padding: 0;
  line-height: normal;
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 99990; /* 提高层级，遮住 TabBar */
}

.post-modal {
  position: fixed;
  left: 0;
  right: 0;
  bottom: -100%;
  background: #fff;
  border-radius: 40rpx 40rpx 0 0;
  z-index: 99999; /* 提高层级，确保在最上层 */
  padding: 0 40rpx 40rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom)); /* 底部安全区域适配 */
  transition: bottom 0.3s ease;
  overflow-y: auto; /* 允许内容滚动 */
  max-height: 85vh; /* 防止过高 */
}

.post-modal.modal-show {
  bottom: 0;
}

.modal-top-bg {
  width: 120%;
  height: 180rpx;
  margin-left: -10%;
  margin-bottom: 20rpx;
  display: block;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30rpx;
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  position: relative;
}

.wish-input {
  width: 100%;
  height: 200rpx;
  background: #f8f8f8;
  padding: 20rpx;
  box-sizing: border-box;
  border-radius: 12rpx;
  font-size: 28rpx;
}

.word-count {
  text-align: right;
  font-size: 24rpx;
  color: #999;
  margin-top: 10rpx;
}

.section-title {
  margin-top: 30rpx;
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
}

.category-tags {
  display: flex;
  flex-wrap: wrap;
  margin-top: 20rpx;
}

.tag-item {
  padding: 10rpx 30rpx;
  background: #f0f0f0;
  border-radius: 30rpx;
  font-size: 26rpx;
  color: #666;
  margin-right: 20rpx;
  margin-bottom: 20rpx;
  transition: all 0.3s;
}

.tag-active {
  background: #7C191E;
  color: white;
}

.anonymous-switch {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 30rpx;
  font-size: 28rpx;
  color: #333;
}

.modal-footer {
  margin-top: 40rpx;
}

.submit-btn {
  background: #7C191E;
  color: white;
  border-radius: 44rpx;
  font-size: 32rpx;
}

